//替换字符串中的所有空格
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void replaceBlank(char s[], int length)
{
    if (s == NULL || length <= 0)
        return ;
    
}

int main(int argc, const char *argv[])
{
        
    
    return 0;
}
